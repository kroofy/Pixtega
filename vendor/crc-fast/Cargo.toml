[package]
name = "crc-fast"
version = "1.10.0"
edition = "2021"
license = "MIT"
description = "Tiny crc-fast stand-in: the three algorithms aws-smithy-checksums calls, no digest 0.10."
publish = false

[dependencies]
crc = "3"
