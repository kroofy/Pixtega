/**
 * `@pixtega/url` re-exports the `pixtega` package unchanged. It exists so
 * both the unscoped name and the `@pixtega` scope resolve to the same
 * implementation; depend on whichever name you prefer.
 */
export * from "pixtega";
